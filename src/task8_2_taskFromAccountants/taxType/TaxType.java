package task8_2_taskFromAccountants.taxType;

public class TaxType {
    //Класс тип налога
    public double calculateTaxFor(double amount) {
        // ничего не считаем тк есть счет без налогов - выводим 0
        return 0.0;
    }
}
